{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*",
    "benchmark"
  ],
  "devDependencies": {
    "@biomejs/biome": "^2.5.7",
    "@changesets/cli": "^3.0.0",
    "typescript": "^7.0.2"
  },
  "scripts": {
    "cs:ver": "changeset version",
    "cs:pub": "changeset publish",
    "cs:add": "changeset add"
  }
}
