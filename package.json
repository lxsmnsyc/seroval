{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*",
    "benchmark"
  ],
  "devDependencies": {
    "@biomejs/biome": "^2.5.7",
    "@changesets/cli": "^2.31.1",
    "typescript": "^7.0.2"
  },
  "scripts": {
    "cs:ver": "changeset version",
    "cs:pub": "changeset publish",
    "cs:add": "changeset add"
  }
}
